<style>
.userUI {
        width: 340px;
        min-width: 340px;
        float: right;
        text-align: right;
        margin-top: 30px;
        padding: 0;
}
.title {
        width: 250px;
        min-width: 250px;
        padding: 0;
}
.col {
        display: inline-block;
}
</style>

<div class="mt-4"> 
        <div class="col title">
                <h1 class="font-weight-light text-left mb-0">
                        <a href="/">Neat Gallery</a>
                </h1>
        </div>
        <div class="col userUI mb-0">
                <% if (me == '') { %>
                        <a href="/login/form">로그인 </a>&nbsp;/&nbsp;
                        <a href="/register/form">회원가입 </a>
                <% }
                else { %>
                        <b><%= me %></b>&nbsp;&nbsp;&nbsp;&nbsp;
                        <a href="/mypage/<%= me %>">My page </a>&nbsp;/&nbsp;
                        <a href="/logout">로그아웃 </a>&nbsp;/&nbsp;
                        <a href="/edit">글쓰기 </a>
                <% } %>
        </div>
</div>