extends layout

mixin message(opp, name)
    if(opp)
        h1.greeting welcome board
            span=name 

append links 
    li(style="float: right;")
        a(href="/logout") Logout
    li(style="float: right;")
        a(href="/mypage") 정보수정
    
block content
    .write-panel
        include includes/upload-form
    .infowrite-panel
        include includes/photo-list
