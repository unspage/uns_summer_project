extends layout

mixin message(opp, name)
    if(opp)
        h1.greeting board
            span=name     

append links 
     
    li(style="float: right;")
        a(href="/logout") Logout 
    
block content
    .dashboard 
        .user 
            +message(opp, name)
    .infowrite-panel
        include includes/list
        



append links 
    li(style="float: right;")
        a(href="/mypage") 정보수정
    li(style="float: right;")
        a(href="/write") 글쓰기

